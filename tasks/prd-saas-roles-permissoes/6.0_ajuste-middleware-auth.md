# Tarefa 6.0: Ajuste do `middleware/auth`

<critical>Ler os arquivos de prd.md e techspec.md nesta pasta. Se você não ler esses arquivos, sua tarefa será invalidada.</critical>

## Visão Geral

Simplificar o middleware `auth.ts` existente para que cuide exclusivamente de autenticação (verificar se usuário está logado), removendo a lógica de validação de roles que agora é responsabilidade do `role-guard.ts`. Esta tarefa é simples mas crítica — garante separação limpa de responsabilidades entre auth e autorização.

<skills>
### Conformidade com Skills Padrões

Nenhuma skill específica necessária além das convenções Vue/Nuxt do projeto.
</skills>

<requirements>

### RF-04
O middleware de autenticação existente deve ser estendido para validar roles além de apenas autenticação (nota: na prática, será **separado** — auth cuida de autenticação, role-guard de autorização)

### RF-10
Rotas públicas (landing page, login, registro) não devem exigir validação de role

</requirements>

## Subtarefas

- [x] 6.1 Ler `middleware/auth.ts` existente
- [x] 6.2 Verificar que não há lógica de roles no middleware (confirmado: zero referências a roles/permissions)
- [x] 6.3 Manter apenas: se usuário NÃO está logado E rota é protegida → redirect para `/auth/login`
- [x] 6.4 Manter: se usuário está logado E rota é auth (login/register) → redirect para `/dashboard`
- [x] 6.5 `protectedRoutes` contém apenas rotas que exigem auth (sem role) — já correto
- [x] 6.6 Middleware é aplicado globalmente (não Named middleware) — já correto
- [x] 6.7 Verificar que rotas `/exercises/*` não causam conflito com módulo Supabase (conflito documentado no QWEN.md, comportamento mantido)

## Detalhes de Implementação

Consultar a **Tech Spec** nas seções:
- **Sistemas Existentes Modificados** → entrada `middleware/auth.ts`
- Visão Geral do Middleware existente → ler relatório do agent de exploração

O middleware após ajuste deve ficar com ~10 linhas:
```ts
export default defineNuxtRouteMiddleware((to) => {
  const user = useSupabaseUser()
  
  const protectedRoutes = ['/dashboard', '/workouts', '/exercises', '/profile', '/settings', '/schedule', '/progress']
  const isProtectedRoute = protectedRoutes.some((route) => to.path.startsWith(route))
  
  if (isProtectedRoute && !user.value) {
    return navigateTo('/auth/login')
  }
  
  const authRoutes = ['/auth/login', '/auth/register']
  const isAuthRoute = authRoutes.includes(to.path)
  
  if (isAuthRoute && user.value) {
    return navigateTo('/dashboard')
  }
})
```

## Critérios de Sucesso

- [x] `middleware/auth.ts` não contém lógica de roles (apenas autenticação) — verificado via grep
- [x] Usuário não logado tentando acessar `/dashboard` → redirect para `/auth/login` (comportamento existente)
- [x] Usuário logado tentando acessar `/auth/login` → redirect para `/dashboard` (comportamento existente)
- [x] Usuário não logado pode acessar `/` (landing page)
- [x] Usuário não logado pode acessar `/auth/confirm`
- [x] Código é simples e legível — sem mudanças necessárias
- [x] Middleware roda em cadeia com `role-guard` (auth primeiro, role-guard depois)

## Testes da Tarefa

- [ ] **Teste de integração**: Usuário não logado navega para `/dashboard`; verificar redirect para `/auth/login` (coberto na task 10.0)
- [ ] **Teste de integração**: Usuário logado navega para `/auth/login`; verificar redirect para `/dashboard` (coberto na task 10.0)
- [ ] **Teste de integração**: Usuário não logado navega para `/`; verificar que NÃO é redirecionado (coberto na task 10.0)
- [ ] **Teste de integração**: Usuário logado sem roles navega para rota com `requiredRoles`; verificar que `auth.ts` permite e `role-guard` barra (coberto na task 10.0)
- [x] **Regression**: `pnpm nuxt prepare` executa sem erros ✅

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `middleware/auth.ts` (modificado)
- `middleware/role-guard.ts` (trabalha em conjunto)
- `nuxt.config.ts` (configuração do Supabase redirect)
- `tasks/prd-saas-roles-permissoes/prd.md`
- `tasks/prd-saas-roles-permissoes/techspec.md`
