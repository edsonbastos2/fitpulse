# Tarefa 1.0: Tipos e Constantes de Roles

<critical>Ler os arquivos de prd.md e techspec.md nesta pasta. Se você não ler esses arquivos, sua tarefa será invalidada.</critical>

## Visão Geral

Criar a base tipada do sistema de roles: interfaces TypeScript em `types/index.ts` e constantes tipadas em `utils/role-constants.ts`. Este é o alicerce que todos os outros componentes dependem — sem esses tipos, nada compila corretamente.

<skills>
### Conformidade com Skills Padrões

Nenhuma skill específica necessária para esta tarefa (tipos TypeScript puros).
</skills>

<requirements>

### RF-05
O sistema deve suportar 3 roles iniciais: `superadmin`, `user`, `personal_trainer`

### RF-06
O sistema deve permitir consulta eficiente de permissões por role para validação em APIs

</requirements>

## Subtarefas

- [x] 1.1 Criar `utils/role-constants.ts` com `ROLES`, `PERMISSIONS` e `ROLE_PERMISSIONS` como `const` objects tipados
- [x] 1.2 Adicionar interfaces `Role`, `UserRole`, `RolePermission`, `TrainerStudent` em `types/index.ts`
- [x] 1.3 Estender interface `UserProfile` com campos opcionais `roles` e `active_role`
- [x] 1.4 Estender `PageMeta` do Nuxt para suportar `requiredRoles` e `requiredPermissions`
- [x] 1.5 Executar verificação de tipos (`nuxt typecheck` ou `tsc --noEmit`)

## Detalhes de Implementação

Consultar a **Tech Spec** nas seções:
- **Interfaces Principais** → código completo de `utils/role-constants.ts`
- **TypeScript Types** → interfaces para `types/index.ts`

Não repetir o código aqui — seguir fielmente o que está na techspec.

## Critérios de Sucesso

- [x] `utils/role-constants.ts` exporta `ROLES`, `PERMISSIONS`, `ROLE_PERMISSIONS`, tipo `Role` e tipo `Permission`
- [x] `types/index.ts` inclui interfaces `Role`, `UserRole`, `RolePermission`, `TrainerStudent`
- [x] `UserProfile` estendido com `roles?: Role[]` e `active_role?`
- [x] Module augmentation de `PageMeta` funciona sem erros
- [x] Zero erros de tipo ao rodar `tsc --noEmit` nos arquivos novos (erros pré-existentes em composables não relacionados)
- [x] Constante `ROLE_PERMISSIONS` contém permissões para todas as 3 roles
- [x] `pnpm nuxt prepare` executa com sucesso

## Testes da Tarefa

- [x] **Teste de unidade**: Verificar que `ROLE_PERMISSIONS.superadmin` contém todas as permissões (documentado em `role-constants.test.ts` — Teste 1)
- [x] **Teste de unidade**: Verificar que `ROLE_PERMISSIONS.user` contém apenas `VIEW_OWN_WORKOUTS`, `VIEW_OWN_PROGRESS`, `REQUEST_UPGRADE` (Teste 2)
- [x] **Teste de unidade**: Verificar que `ROLE_PERMISSIONS.personal_trainer` contém permissões de aluno além das pessoais (Teste 3)
- [x] **Teste de tipo**: Rodar `tsc --noEmit` e confirmar zero erros nos arquivos novos (erros pré-existentes em `useAuth`, `useRecommendations`, `useWorkouts` não relacionados)
- [x] **Teste de integração**: Importar constantes e tipos em outro módulo e verificar que compilam corretamente (`pnpm nuxt prepare` passou sem erros)

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `utils/role-constants.ts` (novo)
- `types/index.ts` (modificado)
- `nuxt.config.ts` (verificar configuração de tipos)
- `tasks/prd-saas-roles-permissoes/prd.md`
- `tasks/prd-saas-roles-permissoes/techspec.md`
