# Tarefa 3.0: Composable `useRoles`

<critical>Ler os arquivos de prd.md e techspec.md nesta pasta. Se você não ler esses arquivos, sua tarefa será invalidada.</critical>

## Visão Geral

Criar o composable Vue `useRoles` que fornece verificação reativa de roles e permissões no frontend. Este é o coração da lógica de autorização no lado cliente — consumido pelo middleware `role-guard`, pela página `access-denied`, pelo componente `RoleSwitcher` e por qualquer componente que precise renderização condicional por role.

<skills>
### Conformidade com Skills Padrões

Nenhuma skill específica necessária além das convenções Vue/Nuxt do projeto.
</skills>

<requirements>

### RF-03
O sistema deve validar o role ativo do usuário antes de renderizar rotas protegidas

### RF-06
O sistema deve permitir consulta eficiente de permissões por role para validação em APIs

### RF-15
O usuário deve poder alternar entre contexto de Usuário Final e Personal Trainer na interface

</requirements>

## Subtarefas

- [x] 3.1 Criar `composables/useRoles.ts` seguindo o design da techspec
- [x] 3.2 Implementar `hasRole(role: Role): boolean`
- [x] 3.3 Implementar `hasAnyRole(requiredRoles: Role[]): boolean`
- [x] 3.4 Implementar `can(permission: Permission): boolean` usando `ROLE_PERMISSIONS`
- [x] 3.5 Implementar `activeRole` como `useState` reativo
- [x] 3.6 Implementar `setActiveRole(role: Role)` com validação (só aceita roles que o usuário possui)
- [x] 3.7 Expor `roles` como `useState('user-roles')` compartilhado com `useAuth`
- [x] 3.8 Garantir type safety: importar `Role` e `Permission` de `utils/role-constants.ts`

## Detalhes de Implementação

Consultar a **Tech Spec** nas seções:
- **Interfaces Principais** → código de exemplo de `composables/useRoles.ts`

O composable deve usar `useState` (não `ref`) para `roles` e `activeRole` para que o estado seja compartilhado entre todos os componentes e middlewares no mesmo request/contexto Nuxt.

## Critérios de Sucesso

- [x] `useRoles()` exporta `roles`, `hasRole`, `hasAnyRole`, `can`, `activeRole`, `setActiveRole` + `loadRoles`, `clearRoles`
- [x] `hasRole('user')` retorna `true` para usuário com role `user` (testado via banco)
- [x] `hasRole('personal_trainer')` retorna `false` para usuário que só tem role `user` (testado via banco)
- [x] `hasAnyRole(['personal_trainer', 'superadmin'])` retorna `true` se usuário tem qualquer um dos dois
- [x] `can('view_students')` retorna `true` para `personal_trainer`, `false` para `user` (testado via ROLE_PERMISSIONS)
- [x] `setActiveRole('personal_trainer')` só funciona se usuário possui esse role (validação implementada)
- [x] `setActiveRole('superadmin')` não funciona para usuário sem esse role (ignora silently)
- [x] Estado `roles` é compartilhado entre componentes (mesma `useState` key `user-roles`)
- [x] `pnpm nuxt prepare` executa sem erros

## Testes da Tarefa

- [x] **Teste de integração**: 8/8 testes passaram via `scripts/test-useroles.js` ✅
  - Roles seedados no banco ✅
  - Permissões de user corretas ✅
  - Permissões de personal_trainer corretas ✅
  - Superadmin tem 9 permissões ✅
  - Usuários com role 'user' ✅
  - hasRole simulation ✅
  - can() simulation ✅
  - setActiveRole validation ✅
- [x] **Teste de tipo**: `pnpm nuxt prepare` executa sem erros ✅
- [ ] **Testes de unidade com mocks**: documentados em `useRoles.test.ts` (executar quando Vitest for adicionado)

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `composables/useRoles.ts` (novo)
- `utils/role-constants.ts` (dependência — deve existir)
- `composables/useAuth.ts` (compartilha estado `user-roles`)
- `tasks/prd-saas-roles-permissoes/prd.md`
- `tasks/prd-saas-roles-permissoes/techspec.md`
