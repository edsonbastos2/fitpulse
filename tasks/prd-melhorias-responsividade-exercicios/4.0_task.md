# Tarefa 4.0: Reorganizar layout da página /exercises

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Visão Geral

Reorganizar a estrutura de layout da página `pages/exercises/index.vue` para integrar os novos componentes (chips, card compacto) e reposicionar search bar e filtros.

<skills>
### Conformidade com Skills Padrões

- **ui-ux-pro-max**: Hierarquia visual clara, espaçamento consistente, fluxo de interação intuitivo
</skills>

<requirements>
- Layout: Header → Search → Filter Chips → Grid → Paginação
- Sidebar lateral oculta no mobile, visível no desktop (≥1024px)
- Manter sincronização com URL query params (comportamento existente)
- Manter estados de loading, error e empty state
- Manter paginação "Load More" funcional
</requirements>

## Subtarefas

- [ ] 4.1 Reorganizar template: mover search para topo (acima dos filtros)
- [ ] 4.2 Integrar `ExerciseFilterChips` no layout mobile
- [ ] 4.3 Manter `ExerciseFilters` como sidebar no desktop (hidden no mobile)
- [ ] 4.4 Ajustar espaçamento entre seções (gap responsivo)
- [ ] 4.5 Verificar estados de loading/error/empty com novo layout
- [ ] 4.6 Testar sincronização de URL com filtros
- [ ] 4.7 Testar responsividade completa (mobile → desktop)

## Detalhes de Implementação

Consultar `techspec.md` → seção "Arquivos Relevantes" → `pages/exercises/index.vue`.

Estrutura do template:
```html
<div class="space-y-6 animate-fade-in">
  <!-- Page Header -->
  <h1>Catálogo de Exercícios</h1>

  <div class="flex gap-6">
    <!-- Desktop Sidebar (hidden no mobile) -->
    <ExerciseFilters class="hidden lg:block" ... />

    <!-- Main Content -->
    <main class="flex-1 min-w-0">
      <!-- Search Bar (largura total) -->
      <ExerciseSearchBar v-model="searchTerm" />

      <!-- Filter Chips (mobile/tablet) -->
      <ExerciseFilterChips 
        :filters="filters"
        @update:mobile-open="isDrawerOpen = $event"
      />

      <!-- Grid com cards compactos -->
      <ExerciseGrid ... />

      <!-- Paginação -->
      <ExercisePagination ... />
    </main>
  </div>
</div>
```

## Critérios de Sucesso

- Header → Search → Chips → Grid em ordem correta
- Sidebar lateral visível apenas no desktop (≥1024px)
- Search bar ocupa largura total no mobile
- Filtros funcionam (chips atualizam ao filtrar)
- URL query params sincronizam com filtros
- Loading, error e empty states funcionam corretamente
- Paginação "Load More" carrega mais exercícios

## Testes da Tarefa

- [ ] Testes de integração: Fluxo completo de filtro + busca + grid
- [ ] Testes de responsividade: Mobile (375px), Tablet (768px), Desktop (1440px)
- [ ] Testes de URL sync: Adicionar ?difficulty=hard na URL → filtro aplicado
- [ ] Testes de estado: Loading skeleton, empty state, error state
- [ ] Testes E2E (Playwright): Fluxo completo no mobile

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `pages/exercises/index.vue` (MODIFICADO)
- `components/catalog/ExerciseFilterChips.vue` (novo)
- `components/catalog/ExerciseGrid.vue` (modificado na task 3.0)
- `components/catalog/ExerciseSearchBar.vue` (modificado na task 5.0)
- `composables/useExercises.ts` (sem mudanças)
